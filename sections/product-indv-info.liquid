{% comment %}
  Markup lives in snippets/product-indv-info-block.liquid so it can be reused
  verbatim by the Hydrocortisone Lander page template. `product`, `section`, and
  `current_variant` are inherited through the shared include scope.
{% endcomment %}
{% include 'product-indv-info-block' %}

{% schema %}
  {
    "name": "Product Info Section",
    "settings": [
      {
        "id": "title_override",
        "type": "text",
        "label": "Title Override"
      }
    ],
    "presets": [
      {
        "name": "Product Info",
        "category": "Products page"
      }
    ]
  }
{% endschema %}
